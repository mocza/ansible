- name: Install the Python package proxmoxer
  become: yes
  hosts: all
  tasks:
    - name: Install proxmoxer package using pip
      pip:
        name: proxmoxer
        state: present
        executable: pip3
